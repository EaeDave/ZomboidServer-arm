name: control-plane

on:
  pull_request:
    paths:
      - "panel/**"
      - "scripts/boot-retry.sh"
      - "scripts/boot-retry.test.sh"
      - "scripts/common.sh"
      - "scripts/pz-agent*.sh"
      - "templates/zomboid-agent.service"
      - ".github/workflows/panel.yml"
      - ".dockerignore"
  push:
    branches:
      - main
      - fex-arm64
      - "feat/**"
    paths:
      - "panel/**"
      - "scripts/boot-retry.sh"
      - "scripts/boot-retry.test.sh"
      - "scripts/common.sh"
      - "scripts/pz-agent*.sh"
      - "templates/zomboid-agent.service"
      - ".github/workflows/panel.yml"
      - ".dockerignore"

permissions:
  contents: read

jobs:
  panel:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Verify host boot telemetry scripts
        run: |
          bash -n scripts/common.sh scripts/boot-retry.sh scripts/boot-retry.test.sh install.sh pzctl
          bash scripts/boot-retry.test.sh
      - uses: docker/setup-buildx-action@v3
      - name: Build and test control-plane image
        run: docker build --file panel/Dockerfile --tag zomboid-control-plane:ci .
